package org.example.model;

import java.util.Comparator;

public class CustomComparator<Student> implements Comparator<Student> {

    @Override
    public int compare(Object o1, Object o2) {
        return 0;
    }
}
